---
scope: bmad-multi-user system
purpose: architecture spine for independent builders
altitude: initiative
updated: 2026-09-24T19:26
---

- (decision) Working mode: Coaching path
- (constraint) Form-factor: installable BMAD Module (skills+scripts+conventions in git); no UI/SaaS; no Core BMAD patch/fork
- (constraint) PRD locked: native BMAD statuses only; Dependency Gates hard-block Build; Claim soft-check v1; Correct-Course = status recalc + impact review; Project Truth = docs+status+code alignment
- (constraint) Hard outs: real-time cursors, SaaS collab, enterprise ACL, replace-git, invent parallel status vocab, smart merge assist deferred
- (event) Workspace bound; PRD+brief inherited as constraints; awaiting deliverable purpose then coaching
- (decision) Deliverable: spine only — no discussion deck, C4, or fuller architecture prose
- (event) Coaching started: paradigm elicitation
- (decision) Paradigm: Preflight-gate over shared truth — Module reads Core BMAD artifacts + thin Module-owned git state; Hard Block on Build/coherent-merge; no orchestrator engine; no derived SoT index. Prevents dual-write drift and AI-DLC-clone overbuild.
- (event) Coaching: paradigm locked; eliciting Module boundaries / composition with Core
- (decision) Core composition: Hard Block via BMAD custom hooks (activation_steps_prepend etc. in _bmad/custom/*.toml) on bmad-build and bmad-correct-course; Module UX skills for status/claim/deps; no git-hook enforcement; wrappers not sole gate. Prevents Core vendor edits and silent Build bypass of primary entry.
- (event) Coaching: Core boundary locked; eliciting Module-owned state location and write ownership
- (decision) Module-owned state home: single tree under _bmad-output/ (e.g. multi-user/ or coordination/) holding deps, Layer Order, Claims, impact-review signal as plain git text. Core Planning Artifacts read-mostly; no scatter of Module fields into Story/PRD frontmatter as primary SoT; no side-root outside _bmad-output. Prevents dual homes and re-hotspotting shared artifact files.
- (event) Coaching: state home locked; eliciting write-ownership / mutation rules
- (decision) Write ownership: Module home = Module-only writes; Planning Artifacts = humans+Core write, Module read-only for statuses; sprint-status/Story status = humans+Core, Module may write only via explicit Correct-Course/recalc using native BMAD status values; vendor Core paths = never. Prevents parallel lifecycle enums and unauthorized Core mutation.
- (event) Coaching: write ownership approved; eliciting script stack then Correct-Course signal
- (decision) Script stack: Python >=3.11 via uv run; Module home state in YAML (+ Markdown for impact-review prose); skills orchestrate UX, scripts own deterministic gate/claim/status with machine+human reasons; no Node/Go gate runtime; no mandatory network service. Aligns with Core BMAD _bmad/scripts; prevents schema-weak shell-only gates.
- (version) uv ~0.12.x (verified web 2026-09); Python requires-python >=3.11 matching Core BMAD scripts
- (event) Coaching: stack locked; eliciting Correct-Course trigger and coherent-merge signal (PRD open Q1-Q2)
- (decision) Correct-Course trigger: every bmad-correct-course run (via custom hook) requires impact review + status recalc in v1 — no touch-heuristic skip, no manual significant flag. Prevents silent Project Truth drift from 'small' changes.
- (decision) Coherent-merge / Build-ready signal: Module home impact-review artifact (YAML and/or MD+frontmatter) with affected items and status open|cleared; Hard Block while any required impact review is open. Prevents ambiguous 'review done' chat claims.
- (event) Coaching: Correct-Course locked; eliciting Layer Order override + dependency declaration shape
- (decision) Layer Order loosen override: named per-story exceptions in Module home layer-order file (story id, which earlier-layer constraints relaxed, reason, approved_by); tighten via FR-5 deps only; no global strict|relaxed mode; no silent chat override. Prevents undocumented Hard Block bypass while allowing audited pilot exceptions.
- (event) Coaching: Layer override locked; eliciting dependency declaration shape
- (decision) Dependency declarations: per-Story files under Module home (e.g. deps/<story-id>.yaml) with depends_on_stories, depends_on_artifacts, layer, or explicit none. Enrollment = file present for every sprint-status Story; missing file = incomplete Hard Block. Cycle/Layer checks scan the deps tree. Rejected single monolithic dependencies.yaml for v1 to cut parallel-declare merge conflicts. Prevents two builders inventing incompatible dep homes.
- (event) Coaching: dep shape locked to per-story files; eliciting Claims model
- (decision) Claims: central Module-home registry (claims.yaml or small path-prefix set) with path_or_story_slice, claimant, active/expiry, updated_at; soft-check warns on intersecting edits/preflight; never hard-locks git. No per-path .claim sidecar as primary SoT. Prevents dual claim ledgers and accidental hard-lock scope creep.
- (event) Coaching: Claims locked; eliciting single gate evaluator + Planning Artifact ready rules
- (decision) Single gate evaluator: one shared script/library implements Hard Block rules; all hooks (bmad-build), status report, and Correct-Course recalc must call it — skills must not reimplement gate logic. Prevents divergent pass/fail across entrypoints.
- (decision) Planning Artifact readiness: Module ships default readiness map artifact-kind → acceptable native BMAD statuses (e.g. PRD final, brief complete, architecture final); project may override map in Module home config; never invent parallel enums. Prevents per-Story ad-hoc ready thresholds and draft-as-ready looseness.
- (event) Coaching: evaluator+readiness locked; eliciting install/package boundary and deferred set
- (decision) Package boundary: installable complementary BMAD module (skills + scripts + template _bmad/custom hooks); never edits vendor Core paths; project Module home at _bmad-output/multi-user/ (seed name); semver on Module-home schema and gate behavior with migration notes on major; offline-only for F1–F5. Prevents ad-hoc skills-folder installs and SaaS/network coupling.
- (assumption) Module home folder name multi-user/ is seed — rename OK if install conventions prefer another token, as long as single home under _bmad-output remains.
- (event) Coaching: package locked; presenting Deferred set for confirm before distill
- (decision) Deferred confirmed: smart merge; hard claim locks; multi-repo; Correct-Course touch-heuristic; claims/deps micro-schema details; pilot SM diary/SM-C1 proxy; exact readiness-map rows (defaults at build). Ops envelope: no deploy/SaaS — local git module; compatibility declared vs Core BMAD majors (NFR-2).
- (event) Finalize started: distill spine from memlog
- (event) Reviewer Gate: lint clean; reconcile+rubric+tech+adversarial run; autofixed AD-2 build-auto, AD-5 single CLI, AD-6/7 layer membership split, AD-8 ids+high-churn, AD-9 impact-review.yaml SoT, AD-10 PA-only map, AD-11 smoke, AD-12 NFR-3
- (decision) AD tightenings from gate: all Build entrypoints hooked (incl bmad-build-auto); one evaluator CLI for gates+report+coherent-merge; story ids=sprint-status keys; layer membership only in deps files; impact-review.yaml sole open|cleared SoT; readiness map PA-only; install smoke; NFR-3 fast preflight AD-12
- (event) spine finalized
